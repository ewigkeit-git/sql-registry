## sql-registry の思想

sql-registry は、SQL を実装コードの奥に埋もれた文字列ではなく、レビュー可能な実装資産として扱うためのライブラリである。

このライブラリは、SQL を Markdown 上で名前付きの定義として管理し、説明、パラメータ、方言差分、限定された動的構造を同じ場所で扱えるようにする。
目的は、SQL を安全に実行することだけではなく、人間と AI のどちらが書いた SQL であっても、読み返し、レビューし、保守できる形に戻すことにある。

sql-registry は、実行時入力が SQL 構文そのものに入り込まないことを重視する。
値は bind parameter として渡し、並び替えやページングも制御された仕組みで扱い、自由な文字列連結や無制限な動的 SQL は採らない。

このライブラリは ORM の代替ではなく、マイグレーション実行ツールでもなく、テーブル設計標準そのものを提供するものでもない。
責務は、実装で使う SQL を安全に、説明可能に、レビューしやすい形で保持し、利用できるようにすることにある。

機能追加や仕様変更を行うときは、sql-registry を重いフレームワークへ広げるのではなく、
「SQL を実装資産として扱う」という中心思想を強める方向かどうかを基準に判断すること。

Do:
- SQL の可読性、説明可能性、レビューしやすさを高める
- パラメータ、方言差分、限定的な動的 SQL を明示的に扱う
- AI 生成 SQL も人間が責任を持てる形へ整理する

Don't:
- 自由な文字列連結や識別子の動的注入を許す
- ORM、マイグレーション、設計標準の責務まで抱え込む
- 便利さのためにレビュー性と安全性を崩す